info:
  name: create universite
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080http://localhost:8080
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
